{
  "name": "employers-app",
  "version": "1.0.0",
	"description": "My first application built on React js during the training courses on JS and React. The application is a record of employees in the company. The application contains: number of employees, search, filters, employee viewing, salary assignment, promotion assignment and bonus, as well as the removal and addition of new employees.",
	"author": "Alexander A.",
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/user-event": "^14.4.3",
    "npm-check-updates": "^16.10.16",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-id-generator": "^3.0.2",
    "react-scripts": "5.0.1",
    "sass": "^1.64.1",
    "web-vitals": "^3.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
		"check-update": "npm-check-updates"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
